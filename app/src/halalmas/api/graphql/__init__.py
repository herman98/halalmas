from graphene_django import DjangoObjectType
import graphene

# PLEASE WRITE GLOBAL SCHEMA FOR GRAPHQL, BELOW
# class Query(graphene.ObjectType):
#     customer = graphene.List(CustomerQL)

#     count = graphene.Int()

#     def resolve_branch(self, info):        
#         return Customer.objects.filter(delstatus=False)[1:5]
    

# schema = graphene.Schema(query=Query)
