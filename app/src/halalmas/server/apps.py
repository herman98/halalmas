from __future__ import unicode_literals

from django.apps import AppConfig


class ServerConfig(AppConfig):
    name = 'halalmas.server'
    verbose_name = 'halalmas.marikoding.com Server for tempat dot com system'
