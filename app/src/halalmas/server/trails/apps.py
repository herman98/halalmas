from __future__ import unicode_literals

from django.apps import AppConfig


class TrailConfig(AppConfig):
    name = 'halalmas.server.trails'
    verbose_name = 'halalmas.marikoding.com Server for audit trails / tracking'
